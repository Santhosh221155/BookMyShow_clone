#pragma once
#include <string>

// Global Enums
enum class SeatType { SILVER, GOLD, PLATINUM };
enum class SeatStatus { AVAILABLE, BOOKED, RESERVED };
enum class BookingStatus { PENDING, CONFIRMED, CANCELLED };
enum class Genre { ACTION, ROMANCE, COMEDY, HORROR };